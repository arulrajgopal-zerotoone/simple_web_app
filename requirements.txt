fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==22.0.0
jinja2==3.1.4
python-multipart==0.0.9
sqlalchemy==2.0.35
pyodbc==5.1.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4's bcrypt backend probes for a legacy wrap bug using bcrypt
# internals that were removed/changed in bcrypt>=4.1 (no __about__, stricter
# 72-byte check raises instead of truncating) - pin to a compatible version.
bcrypt==4.0.1
pyjwt==2.9.0
python-dotenv==1.0.1
